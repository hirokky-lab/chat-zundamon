# ずんだもんAI

## 導入・初期設定を頼まれたとき

- 最初に [AI向けセットアップガイド](docs/setup-guide.md) を読む。実際の起動・接続手順の正本は [初期設定](docs/initial-setup.md)。
- 環境と設定の有無を確認し、不足項目だけを案内する。既存の設定・データを消して最初からやり直さない。
- APIキー・秘密鍵・認証コードをチャットへ貼らせない。本人が初期設定画面やサービスの正規画面へ入力する。値をログ・スクリーンショット・Gitへ残さない。
- 導入先・DB・API契約は利用者自身のもの。作者や別プロジェクトの接続先を流用しない。
- 本人操作が必要な場合は、具体的な次の一操作だけを案内する。既に承認された作業を繰り返し確認しない。

## 開発時の基本ルール

- 正本はこの独立リポジトリ。YUI側を編集しない。
- ユーザー承認: ずんだもんの人格・声・Live2D・緑系カラー、温かい吹き出しUI。将来YUIへ差し替えられる共通基盤。
- 開発はローカルを基本とする。公開・接続・課金・既存個人データ移行は現在の依頼で許可された範囲だけ行う。過去の作成時の範囲を、新しい許可や恒久的禁止へ拡大しない。
- 継承した @yui 内部パッケージ名は機械的に一括変更しない。
- 権限・秘密拒否・所有者分離・削除・外部変更の確認を継承する。人格設定から上書き不可。
- 実API、音声、Live2D、本番の成功はそれぞれ実確認する。試作を実装完了と呼ばない。
- 端末内保存キー、DB、Google OAuth、VercelプロジェクトはYUIから分離してから接続する。
- 通常の修正は対象仕様とコードから始め、設計や機能境界を変える場合に docs/design.md を参照する。初期工程を毎回やり直さない。並列の重いビルド・ローカル音声モデルは起動しない。
