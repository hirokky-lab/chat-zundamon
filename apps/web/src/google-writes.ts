export type WriteInput={kind:'gmail';to:string;title:string;body:string;replyId?:string}|{kind:'drive';folderId:string;title:string;body:string};
export type WriteView={id:string;state:'prepared'|'executing'|'succeeded'|'unknown'|'cancelled'|'expired';expiresAt:string;input:WriteInput;location:string;from:string|null;result:{id:string}|null};
export type WritesApi={prepare(input:WriteInput):Promise<WriteView>;status(id:string):Promise<WriteView>;cancel(id:string):Promise<WriteView>;confirm(id:string):Promise<WriteView>};
export function createWritesApi(http:typeof fetch):WritesApi{async function call(action:string,body:unknown){const r=await http('/api/google-writes/'+action,{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error('接続権限や入力内容を確認してください。実行結果が不明な場合は状態を確認してください。');return await r.json() as WriteView;}return {prepare:i=>call('prepare',i),status:id=>call('status',{id}),cancel:id=>call('cancel',{id}),confirm:id=>call('confirm',{id})};}
